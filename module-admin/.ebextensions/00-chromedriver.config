packages:
  yum:
    wget: []
    dpkg: []

commands:
  01_download_chrome:
    command: |
      wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
치
  02_install_chrome:
    command: |
      sudo dpkg -i google-chrome-stable_current_amd64.deb || true

  03_fix_dependencies:
    command: |
      sudo yum --fix-broken install -y

  04_check_chrome_version:
    command: |
      google-chrome --version || true

